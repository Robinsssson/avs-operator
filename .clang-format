# .clang-format file for C++ code style

BasedOnStyle: Google  # 基于 Google 的 C++ 风格
IndentWidth: 4        # 每个缩进级别 4 个空格
ColumnLimit: 100      # 每行最大字符数限制
BreakBeforeBraces: Allman  # 在大括号前换行 (Allman 风格)

# 设置空格样式
SpaceBeforeParens: ControlStatements  # 控制语句的括号前有空格，如 if、while、for
SpaceAfterCStyleCast: true            # C 风格的强制类型转换后保留空格

# 控制数组初始化的样式
AllowAllParametersOfDeclarationOnNextLine: false # 函数声明的参数不换行
BinPackArguments: false                    # 不将函数参数合并在一行

# 继承方式
PointerAlignment: Left  # 指针符号 `*` 放在类型的左侧，例如：`int* ptr`

# 设置类成员变量的排列方式
AlignConsecutiveDeclarations: true  # 对齐连续声明的变量
AlignTrailingComments: true          # 对齐尾随注释

# 类成员函数的定义
ConstructorInitializerAllOnOneLineOrOnePerLine: true  # 构造函数初始化器列表：所有参数在一行或每个参数一行

# 格式化其他元素
SortIncludes: true                # 自动排序头文件
Cpp11BracedListStyle: true        # C++11 列表初始化风格
